class Room {

  String number;
  bool checked;
  bool powerCords;
  bool dividers;
  bool computers;
  String errorMessage;

  Room(this.number, this.checked, this.powerCords, this.dividers,
    this.computers, this.errorMessage);

}